{
  "$schema": "http://json-schema.org/schema",
  "id": "NxAwsCache",
  "title": "Add AWS S3 Cache configuration to NX workspace",
  "type": "object",
  "properties": {
    "awsAccessKeyId": {
      "type": "string",
      "description": "AWS Access Key ID.",
      "x-prompt": "AWS Access Key ID (optional - can be defined using Environment variables)"
    },
    "awsSecretAccessKey": {
      "type": "string",
      "description": "AWS Secret Access Key.",
      "x-prompt": "AWS Secret Access Key (optional - can be defined using Environment variables)"
    },
    "awsRegion": {
      "type": "string",
      "description": "AWS Region",
      "x-prompt": "AWS Region (optional - can be defined using Environment variables)"
    },
    "awsBucket": {
      "type": "string",
      "description": "AWS S3 Bucket, where the cached Nx outputs will be stored",
      "x-prompt": "AWS Bucket (optional - can be defined using Environment variables)"
    }
  }
}
